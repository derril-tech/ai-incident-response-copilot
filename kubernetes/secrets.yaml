# This is a template - actual secrets should be created separately
apiVersion: v1
kind: Secret
metadata:
  name: ai-incident-response-secrets
  namespace: ai-incident-response
type: Opaque
stringData:
  # Database Credentials
  POSTGRES_USER: "incident_user"
  POSTGRES_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  CLICKHOUSE_USER: "default"
  CLICKHOUSE_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  REDIS_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  
  # Storage Credentials
  MINIO_ACCESS_KEY: "CHANGE_ME_IN_PRODUCTION"
  MINIO_SECRET_KEY: "CHANGE_ME_IN_PRODUCTION"
  
  # Encryption Keys
  MASTER_ENCRYPTION_KEY: "CHANGE_ME_IN_PRODUCTION_BASE64_ENCODED"
  JWT_SECRET: "CHANGE_ME_IN_PRODUCTION"
  
  # External Integrations
  JIRA_API_TOKEN: "CHANGE_ME_IN_PRODUCTION"
  SERVICENOW_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  SPLUNK_TOKEN: "CHANGE_ME_IN_PRODUCTION"
  
  # SAML/OIDC Configuration
  SAML_PRIVATE_KEY: "CHANGE_ME_IN_PRODUCTION"
  OIDC_CLIENT_SECRET: "CHANGE_ME_IN_PRODUCTION"
---
apiVersion: v1
kind: Secret
metadata:
  name: tls-secret
  namespace: ai-incident-response
type: kubernetes.io/tls
data:
  # Base64 encoded TLS certificate and key
  tls.crt: LS0tLS1CRUdJTi... # Replace with actual certificate
  tls.key: LS0tLS1CRUdJTi... # Replace with actual private key
